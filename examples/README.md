# Examples

This folder contains minimal examples demonstrating OCP verification.

A typical example includes:

- a file (observation)
- its digest
- a referenced transaction
- a verification result

Altering any byte in the file must cause verification to fail.
